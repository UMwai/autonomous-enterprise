# Autonomous Enterprise - Infrastructure Environment Variables

# LLM Provider API Keys (if using direct API access instead of CLI OAuth)
ANTHROPIC_API_KEY=sk-ant-your-key-here
OPENAI_API_KEY=sk-your-key-here
GOOGLE_API_KEY=your-key-here

# Stripe (for monetization)
STRIPE_API_KEY=sk_test_your-key-here
STRIPE_WEBHOOK_SECRET=whsec_your-secret-here

# Database (managed by Docker, but can be overridden)
# DATABASE_URL=postgresql://ae:ae@localhost:5432/ae

# Redis (managed by Docker, but can be overridden)
# REDIS_URL=redis://localhost:6379

# Temporal (managed by Docker)
# TEMPORAL_HOST=localhost:7233
# TEMPORAL_NAMESPACE=default

# Phoenix Observability (managed by Docker)
# PHOENIX_COLLECTOR_ENDPOINT=http://localhost:4317

# Model Preferences (Premium models only)
TIER1_MODEL=claude-opus-4-5-20251101
TIER2_MODEL=gpt-5.2
TIER3_MODEL=gemini-3-pro-preview

# Budget Settings
DEFAULT_RUN_BUDGET=10.0
MAX_RUN_BUDGET=100.0
